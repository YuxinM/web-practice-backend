spring:
  #环境 dev:开发环境|prod:生产环境
  profiles:
    active: dev #激活的配置文件

  datasource:
    url: jdbc:mysql://106.15.42.179:3303/practice?useSSL=false&rewriteBatchedStatements=true&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  #文件大小限制
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

  #JPA配置
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5Dialect
    show-sql: false
    hibernate:
      ddl-auto: none

#logging:
#  config: classpath:logback.xml

springdoc:
  packagesToScan: com.example.webpractice.controller
